# Examples

Good:

- `feat(skills): serve skill files as skill:// resources`
- `fix(edge): reject JSON-RPC batches before the filter runs`

Bad:

- `Fixed stuff` (no type/scope, not imperative)
- `feat: Added the new skills feature.` (past tense, trailing period)
